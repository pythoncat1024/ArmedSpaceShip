 0. 清空代码目录
 1. make_android
	a. 下载代码
	b. 生成当前下载的代码的 manifest,xml
    c. 获取昨天的每日版本的清单文件路径
	d. 通过比较昨天的 manifest.xml 与 今天的 manifest.xml 生成 commit.json
	e. 通过 commit.json 生成 diff 的 lastModify.html
	f. 修改 buildinfo.sh 中的变量，来修改 daily build 的版本号
	g. 开始编译，执行 make -j8 的操作
	h. 通过 grep "completed successfully" build.log 来确定编译成功或者失败
	i. 编译 ota 包，make otapackage
 2. publish_xxx.sh
	j. 在让开发下载的服务器上面(69.62)上面创建对应的目录
	k. 把 $Date"_"$CIB_S_VARIANT 作为一个字符串写进文本文件 /mnt/$product"_"$branch"_"$CIB_S_VARIANT".txt" 中
	l. 把刷机需要的文件拷贝到 daily build 发布的服务器对应的目录里面 (额外附加 manifest.xml + build.prop + build.log)
		--> [ 这里的 manifest.xml 就成了明天的 对比 manifest.xml ]
	m. 把 ota 升级需要的文件，拷贝到 daily build 发布的服务器对应的目录里面
 3. autocopy2nj
	n. resync 59.62 上面的 daily_build 的内容到 南京对应的服务器上面
	o. 执行发送邮件的脚本，发送邮件。邮件内容就是 版本路径加上 diff 提交的信息